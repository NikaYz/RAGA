# elk_stack/logstash-config.conf
input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

filter {
  # Add parsing here if needed
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "rag-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
